# OukaroManager WebUI

Vue 3 + TypeScript + Vite frontend for the KernelSU module WebUI. The UI manages apps installed for the primary Android user (user 0) and writes static output into the module `webroot/`.

## Commands

```sh
npm install
npm run build
```

`npm run build` writes the static site into `../module/webroot/`.
